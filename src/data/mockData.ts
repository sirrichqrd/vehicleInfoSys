import { Driver, Vehicle, DriverLog } from '../types';

// Empty arrays - ready for your data
export const mockDrivers: Driver[] = [];

export const mockVehicles: Vehicle[] = [];

export const mockLogs: DriverLog[] = [];